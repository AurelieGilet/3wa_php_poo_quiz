#!/usr/bin/env php
<?php

require_once __DIR__ . '/../vendor/autoload.php';

use Console\Commands\BuildJsCommand;
use Symfony\Component\Console\Application;

$app = new Application();

$app->add(new BuildJsCommand());

$app->run();
